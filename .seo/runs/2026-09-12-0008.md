# SEO run — 2026-09-12 00:08 UTC

## Measured

Before this pass, production had four duplicate-title groups: the homepage
and issue 07 index, three splash pages, and the issue 05 and issue 06 index /
splash pairs.

## Chose

`repair` — distinguish the duplicate page titles while preserving the site's
visual language. The title, Open Graph title, and Twitter title are aligned for
the affected pages.

## Did

- Gave the homepage a descriptive archive title.
- Added issue and `Splash` qualifiers to splash-page titles.
- Distinguished the issue 07 index as an archive page.
- Committed as `d743709` and pushed to `origin/main`.
- Deployed through Wrangler; deployment preview:
  `https://40d524cf.plinth-4pc.pages.dev`.

## Verification

- Passed local census: all 87 HTML pages have unique `<title>` values.
- Passed post-deploy health crawl: 0 duplicate-title groups, 0 broken targets,
  and 0 redirecting internal targets.
- The live crawl changed 7 page fingerprints, consistent with the affected
  title metadata and no content or routing repair.

## Not done

- Two duplicate-meta-description groups remain and are the next metadata
  repair: 29 pages in the older issue set and 13 pages in the issue 07 set.
- Fifteen orphan candidates remain in the health report; the main cluster is
  linked through image-map `<area>` elements, which the current audit does not
  count as `<a>` inbound links.

## How the run ended

The duplicate-title improvement is deployed, verified, committed, and pushed.
The next discrete improvement is page-specific meta descriptions.
