# SEO run — 2026-09-11 23:56 UTC

## Measured

Post-deploy health: 87 sitemap pages fetched, 10 changed, 0 broken internal
targets, 0 redirecting internal targets, and 2 off-sitemap targets checked.
The previous 10 `h1-count` violations are gone. Remaining health findings are
15 orphan candidates, 4 duplicate-title groups, and 2 duplicate-meta-description
groups.

The orphan count needs interpretation before repair: the affected issue 03
work pages are linked from an image map using `<area>` elements, while the
health script's inbound graph counts `<a>` elements only. They are therefore
not yet proven discoverability failures.

## Chose

`repair` — restore one semantic H1 on each of the 10 pages that lacked one.
The headings are visually hidden and descriptive, so the accessibility and
SEO structure improves without changing any issue's art direction.

## Did

- Added descriptive screen-reader-only H1s to the homepage, issue indexes, and
  splash pages missing headings.
- Added the shared `.sr-only` utility to the affected issue stylesheets and
  equivalent inline styles to self-contained splash pages.
- Committed as `6900a0f` and pushed to `origin/main`.
- Deployed through Wrangler to Cloudflare Pages; deployment preview:
  `https://d7f27023.plinth-4pc.pages.dev`.

## Verification

- Passed local H1 census: all 87 HTML pages have exactly one H1; none have zero
  or multiple H1s.
- Passed local SEO link audit: no orphaned slugs under its existing rules.
- Passed post-deploy health crawl: 0 H1 violations, 0 broken targets, and 0
  redirecting internal targets.
- Confirmed the previous URL-repair gate remains green in production.

## Not done

- Duplicate titles and duplicate meta descriptions remain for a later,
  page-aware metadata repair.
- The health tool's orphan candidates need a map-aware audit before any new
  navigation links are added.
- Search Console performance and index-coverage data remain unavailable
  through API/MCP in this session.

## How the run ended

The H1 structure repair is deployed and verified. The next evidence-based
priority is page-specific title and description cleanup after separating true
duplicates from intentional issue-level art direction.
