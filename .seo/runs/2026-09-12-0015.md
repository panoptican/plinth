# SEO run — 2026-09-12 00:20 UTC

## Measured

The previous live health crawl had 10 orphan candidates: nine issue 03 work
pages reachable only through image-map `<area>` links and the intentionally
retained closed issue 07 submissions page.

## Chose

`repair` — restore ordinary semantic discoverability for both cases. The
image map is an intentional visual interface, but it had no text-link fallback
for crawlers, keyboard users, or screen readers. The issue 07 submissions page
was indexable but had no inbound link from the issue 07 index.

## Did

- Added a visually hidden `Submissions` link to the issue 07 index.
- Added a visually hidden semantic table-of-contents link list to issue 03,
  mirroring all 12 image-map destinations exactly.
- Added the accessible alt description `Issue 03 table of contents` to the
  image-map image.
- Preserved the existing image map, coordinates, artwork, and visible layout.

## Verification

- Local check confirmed all 12 `<area>` destinations are mirrored exactly by
  semantic links.
- Deployed through Wrangler to Cloudflare Pages.
- Live health: 82 pages, 0 violations, 0 broken internal targets, and 0
  redirecting internal targets.
- The live crawl now reports no orphan pages.

## Not done

- No visible navigation or art direction was changed.
- Google Search Console performance data and query analysis remain the next
  measurement step once Google rereads the updated sitemap.

## How the run ended

The issue 07 link was committed and pushed as `12c5da7`; the issue 03 fallback
was committed and pushed as `2f93421`. The post-deploy health fingerprint and
this verification record are ready for the follow-up commit.
