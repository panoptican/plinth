# SEO run — 2026-09-11 23:21 UTC

## Measured

Search Console sitemap state: verified in the user's open Chrome session for
`sc-domain:plinth.us`. The Sitemaps report shows `https://plinth.us/sitemap.xml`
submitted on Sep 11, 2026, status `Success`, last read Sep 11, 2026, and 87
discovered pages.

Source link census: 276 internal `href` references to `.html` documents across
82 pages. The live contract already serves the corresponding extensionless
URLs; representative checks showed `.html` URLs 308 once to the extensionless
URL and extensionless URLs 200.

Live health after the source edit: 87 sitemap pages fetched, 0 broken internal
targets, 66 redirecting internal targets, and 68 unique internal targets
checked. The redirect count is unchanged because this source edit is not yet
deployed.

## Chose

`repair` — replace redirecting internal `.html` links with the final
extensionless URLs already used by the sitemap, canonicals, and production
routing. This removes internal crawl hops and consolidates links without
changing the art-directed presentation.

## Did

- Repointed all 276 internal document `href` references in 82 source pages.
- Converted `index.html` links to the containing issue directory (`./` or the
  equivalent parent path); removed `.html` from work and utility page links.
- Aligned the SEO config's priority pages and crawl hubs with the extensionless
  production URLs.
- Left social metadata URLs and non-`href` assets unchanged.
- Closed NY-5 after verifying the sitemap receipt in Chrome.
- Added NY-6 to track deployment and the required live re-crawl.

## Verification

- Passed SEO foundation state check: `upgrade_state.py` reports `current` with
  nothing to do.
- Passed source census: 0 remaining internal `href` references contain
  `.html`.
- No new unresolved document links were identified; the SEO link audit reports
  no orphaned slugs. Six pre-existing non-document edge cases remain outside
  this repair's scope.
- `git diff --check` surfaced only existing mixed space/tab indentation on
  touched issue 01 lines; no indentation was introduced by this repair.
- Passed live URL spot checks: old `.html` routes 308 once to extensionless
  routes; extensionless routes return 200.
- Not yet passed the production repair gate: the source is not deployed, so the
  live crawl still reports 66 redirecting internal targets.

## Not done

- No deployment, commit, or push was performed; `.seo/config.json` has Git mode
  `none`, and the SEO workflow does not auto-deploy.
- A post-deploy live health crawl is still required to confirm the redirecting
  target count falls from 66 to 0.
- Existing H1, duplicate metadata, and other page-aware health findings remain
  for later targeted runs.

## How the run ended

The next technical repair is implemented and locally verified, with production
verification intentionally left open behind the deployment gate.
