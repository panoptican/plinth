# SEO run — 2026-09-11 22:48 UTC

## Measured

GSC 28d: not measured; no Search Console property is connected. Pages: index,
ranking, CTR, and demand states unavailable.

Bing: no Webmaster API key configured.

Health: 87 production sitemap pages, all HTTP 200; median TTFB 160 ms, median
HTML 10,614 bytes, 0 pages over 1,000 ms. The first live crawl recorded 251
violations: 87 canonical mismatches, 87 sitemap URL redirects, 39 redirecting
internal links, 21 orphan candidates, 4 duplicate-title groups, 2 duplicate
meta-description groups, and 10 pages without one H1.

Truth: 0 rules, 0 contradictions, 0 missing claims, 0 unchecked.

Claims re-verified: 0; the archive has no dated SEO claim briefs yet.

AI panel: skipped; no DataForSEO or answer-engine data connection.

AI referrals: not measured; no GA4 or access logs configured.

Census: not run; the census requires Search Console data for useful index and
performance verdicts.

Vitals: speed median TTFB 160 ms, 0 slow pages, LCP/INP/CLS unchecked; relevance
not scored; CTR not measured; link health 0 broken targets and 5 redirecting
unique targets in the production crawl, with 21 orphan candidates; backlinks
unchecked.

Radar: no dated demand signals; community-search connection unavailable.
SERP, internal-link opportunity, brand split, competitor, backlink, and
seasonality panels: unchecked or not due because the required connections/data
are absent.

Product: no watched-path commits since the last run record; no product change
signal.

Outcomes: 0 decided actions; no Search Console pulls loaded, so no priors yet.
Debt: 0 prior run not-done items; 4 needs-you items are open.

## Candidates

| Rank | Action | Target | Evidence | Effort | Score |
|---|---|---|---|---|---|
| 1 | repair | Sitemap/canonical URL contract | All 87 sitemap URLs redirect from `.html` to extensionless final URLs, and all 87 canonicals advertise the redirected form. | M | 38 |
| 2 | repair | Redirecting internal issue-directory links | 39 source pages link to `/issueNN` and receive a trailing-slash redirect. | S | 33 |
| 3 | repair | Duplicate titles and descriptions | Four duplicate-title groups and two shared description groups reduce page-level clarity. | M | 25 |

The URL contract repair won because it directly improves crawl efficiency and
canonical clarity across the entire 87-page archive, with deterministic source
evidence and no need to change visible design or prose. It was selected as an
unattended default run; no candidate required paid research.

## Chose

`repair` — align the sitemap, canonical tags, and issue-directory links with the
extensionless URLs that production actually serves.

## Did

- Created the first `.seo/` foundation: config, brand/positioning draft, truth
  ledger, attributes, keyword baseline, link inventory, content ledger,
  roadmap, radar seeds, and needs-you queue.
- Updated all 87 source canonical tags from `.html` URLs to the final
  extensionless URLs (`/` for the homepage and issue indexes; no extension for
  work/about/archive pages).
- Updated the sitemap's 87 `<loc>` values to those final URLs and removed 87
  uniform `lastmod` values that were not independently verifiable as content
  dates.
- Updated issue-directory links to include the trailing slash.
- Added the pre-change production evidence at
  `.seo/evidence/2026-09-11-url-contract-before.md`.

## Verification

- Passed local source check: 87/87 HTML pages have canonical tags; 0 canonical
  values end in `.html`.
- Passed local sitemap check: 87 URLs parse; 0 sitemap URLs end in `.html`; 0
  `lastmod` tags remain.
- Passed local link audit: no pages with fewer than two inbound links were
  reported by the repository scan.
- Passed `git diff --check` for the SEO/source change set.
- Production-shaped verification is **unchecked**: the source is not deployed,
  so the live crawl still reports the pre-change 251 violations. This run did
  not deploy or merge anything.
- Core Web Vitals are **unchecked**; no Lighthouse/PageSpeed connection is
  configured.

## Not done

- The live URL contract cannot be confirmed until this source repair is deployed;
  the next run should re-run the live health diff after deployment.
- Duplicate titles and descriptions were left for a separate, page-aware
  editorial repair because the archive uses issue-specific art direction and
  shared copy changes need content judgment.
- The 21 live orphan candidates were not changed; they need a post-URL-migration
  crawl and Search Console evidence before deciding whether to add links or
  consolidate pages.
- Indexing, CTR, ranking, branded/non-branded demand, and action outcomes remain
  unknown without Search Console.

## Needs you

- Confirm PLINTH's primary audience and whether its issue-level submissions pages
  describe historical calls or an active submissions route.
- Connect Google Search Console for `https://plinth.us/`.
- Confirm the competitor/reference set before comparison or off-page work.
- Review and deploy the local URL-contract repair through the normal workflow;
  deployment was intentionally not performed by this run.

## Spend

No paid calls. DataForSEO, Search Console, Bing, GA4, OpenSEO, and PageSpeed
connections were not available, so their panels were skipped.

## Portfolio

Not a census run; no Search Console-backed portfolio verdicts were available.

## How the run ended

`config.git.mode` is `none`. The working tree contains the uncommitted SEO
foundation and URL-contract repair plus pre-existing unrelated changes in
`.github/workflows/deploy-cloudflare-pages.yml`, `.gitignore`, `AGENTS.md`, and
`app/`; nothing was committed, pushed, merged, or deployed.
