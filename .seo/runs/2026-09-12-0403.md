# SEO run — 2026-09-12 04:03 UTC

Mode: measure
Attended: yes

## Measured

- Inventory: 82 HTML pages across the homepage and seven issues; 81 pages are
  under `src/issue*/`.
- Live health baseline: 82 pages, 0 violations, 0 broken internal targets, and
  0 redirecting internal targets in the latest production crawl.
- Metadata audit: issue indexes and piece pages were compared across
  `<title>`, meta description, Open Graph, Twitter, and canonical values.
- Search Console: the verified `sc-domain:plinth.us` property and submitted
  sitemap remain available as the manual baseline; no API/MCP export was used.
- Speed, CTR, demand, answer-engine, backlink, and competitor panels were not
  refreshed for this metadata-only audit.

## Candidates

| candidate | score | disposition |
|---|---:|---|
| Correct the stale social description on Issue 02 `oleary` | 8 | Deferred; source edit was not requested in this audit. |
| Normalize equivalent HTML entity encoding on Issue 01 `leeduplessis` | 3 | Not actionable; the rendered metadata content is already equivalent. |
| Review Issue 05 Open Graph title pattern across all issue pages | 7 | Deferred; this is a coherent issue-wide legacy pattern, not a one-page defect. |
| Review Issue 06 utility-page Open Graph/Twitter title pattern | 6 | Deferred; this is a repeated issue-level pattern. |
| Review Issue 07 Open Graph/Twitter title pattern across all pieces and utilities | 6 | Deferred; changing one page would violate the site's standardization rule. |
| Measure-only metadata consistency report | 9 | Chosen; it answers the requested audit without making an unsupported partial rewrite. |

## Chose

`measure-only` — complete the issue-wide metadata consistency audit and record
the isolated mismatches and repeated patterns without changing source files.
The requested work was an assessment, and the site's established rule is that
individual-piece title changes must be reviewed as complete issue-wide changes.

## Did

- Compared all 82 HTML pages' title, description, Open Graph, Twitter, and
  canonical metadata fields.
- Confirmed that issue indexes use coherent standardized title families by
  issue, with legacy punctuation differences between issues.
- Confirmed that individual-piece titles are coherent within each issue;
  Issue 07's Lee page matches its issue peers and was not singled out.
- Identified one actual cross-field mismatch:
  - `src/issue02/oleary.html`: Open Graph and Twitter descriptions are
    truncated to `TWO AMANITA ODES by Peter O`, while the page description is
    `TWO AMANITA ODES by Peter O'Leary - PLINTH Issue 02`.
- The Issue 01 `leeduplessis` alert is only an encoding difference: `&amp;` in
  Open Graph values versus `&` in the HTML and Twitter values. The rendered
  metadata content is equivalent, so it is not a content defect.
- Identified repeated patterns deliberately left untouched: Issue 05 Open
  Graph titles, Issue 06 utility-page Open Graph/Twitter titles, and Issue 07
  Open Graph/Twitter titles.
- Source changes: none. `git diff --check`: passed.

## Not done

- No metadata source files were edited. A future repair can be limited to the
  one stale Issue 02 description unless a complete issue-wide metadata
  revision is explicitly approved.
- No issue-wide title rewrite was proposed or applied; that would alter the
  site's standardized editorial system and requires a separate decision.
- No Search Console export/API pull was made; it is not required to identify
  these source-level inconsistencies.

## Needs you

Nothing. The one stale Issue 02 social description is recorded as a candidate,
not a blocked decision, because this run was intentionally assessment-only.

## Spend

Paid calls: 0. Cost shape: none.
