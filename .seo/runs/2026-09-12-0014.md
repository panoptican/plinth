# SEO run — 2026-09-12 00:15 UTC

## Measured

The live health crawl now contains 82 sitemap pages after removing five splash
routes. It reports 0 broken internal targets and 0 redirecting internal
targets. Its remaining 10 orphan candidates are the nine issue 03 work pages
linked through an image map and the intentionally retained closed issue 07
submissions page.

## Chose

`repair` — remove the five orphan splash routes after the user confirmed they
should be retired. This resolves the duplicate visual entry paths while
preserving old inbound URLs through redirects to the issue indexes.

## Did

- Deleted the five splash HTML pages and their page-only image assets.
- Removed the five splash URLs from `src/sitemap.xml`, reducing the sitemap
  from 87 to 82 URLs.
- Removed the unused issue 04 splash-border CSS.
- Added Cloudflare Pages 301 redirects for both `/splash` and `/splash.html`
  forms to each corresponding issue index.
- Left `/issue07/submissions` unchanged and indexable because it clearly states
  that submissions are closed.
- Closed NY-7 with the user's decision.

## Verification

- Local source audit: 82 HTML pages, 82 sitemap URLs, and no source links to a
  splash route.
- Production: both legacy URL forms return 301 for all five issues, each to
  its issue index.
- Production: all five issue indexes return 200.
- Production sitemap: 82 URLs and zero splash entries.
- Live health: 0 broken internal targets and 0 redirecting internal targets.
- The health tool still lists 10 orphan candidates because it does not count
  image-map `<area>` links; the nine issue 03 work pages are known false
  positives, while issue 07 submissions remains intentionally unlinked.

## Not done

- No change was made to the closed issue 07 submissions page; its indexable
  status is intentional and its copy clearly says submissions are closed.
- No further orphan repair was made for the nine issue 03 image-map pages;
  their inbound `<area>` links are present in the source, but the health tool
  does not count that link type.

## How the run ended

The source cleanup was committed and pushed as `2a2fc51` (`seo: remove splash
routes`). The post-deploy health fingerprint and this verification record are
ready for the follow-up commit.
